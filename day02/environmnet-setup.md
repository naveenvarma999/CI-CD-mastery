# Development Environment

## Operating system

Windows with WSL 2

## Required tools

- Git
- Python
- Pip
- Visual Studio Code
- Docker Desktop
- GitHub CLI
- kubectl
- kind
- Helm
- Terraform

## Verification commands

```powershell
git --version
python --version
pip --version
code --version
docker version
gh --version
kubectl version --client
kind version
helm version
terraform version
wsl --status



Docker
↓
Runs containers

kind
↓
Uses Docker containers to create a Kubernetes cluster

kubectl
↓
Sends commands to that Kubernetes cluster